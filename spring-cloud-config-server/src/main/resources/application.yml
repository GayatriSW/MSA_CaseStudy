spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: C:\Users\student\git_2\Config-Repository 

    
server:
  port: 5000

management:
  endpoints:
    web:
      exposure:
        include: "*"
        
        
        